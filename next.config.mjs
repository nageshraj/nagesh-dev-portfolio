/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/nagesh-dev-portfolio',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
